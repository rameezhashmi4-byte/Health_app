<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Sofia Pro font family -->
    <!-- Note: Add Sofia Pro font files (.ttf or .otf) to this directory -->
    <!-- For now, using system font as fallback -->
    <font
        android:fontStyle="normal"
        android:fontWeight="400"
        android:font="@font/sofia_pro_regular" />
    <font
        android:fontStyle="normal"
        android:fontWeight="500"
        android:font="@font/sofia_pro_medium" />
    <font
        android:fontStyle="normal"
        android:fontWeight="600"
        android:font="@font/sofia_pro_semibold" />
    <font
        android:fontStyle="normal"
        android:fontWeight="700"
        android:font="@font/sofia_pro_bold" />
</font-family>
